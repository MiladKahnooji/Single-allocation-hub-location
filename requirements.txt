# Python 3.12 client runtime and verification dependencies.
# Prefer the official CPU wheel index so installation does not pull CUDA
# runtime packages on a normal client workstation.
--extra-index-url https://download.pytorch.org/whl/cpu
matplotlib>=3.9,<4
numpy>=2.0,<3
openpyxl>=3.1,<4
pulp>=3.0,<4
torch>=2.4,<3
pytest>=8,<9
nbformat>=5,<6
nbconvert>=7,<8
ipykernel>=6,<8
